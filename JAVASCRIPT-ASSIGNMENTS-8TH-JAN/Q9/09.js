// truthly values in javascript

let names = "surendra"

if (names) {
   console.log(`${names} is truthly value`); 
} else {
    console.log(`not ${names} is truthly value`);
}

let pi =3.14;

if (pi) {
    console.log(`${pi} is truthly value`);
} else {
    console.log(`not ${pi} is truthly value`);
}

let truth =true;

if (truth) {
    console.log(`${truth} is truthly value`);
} else {
    console.log(`${truth} is truthly value`);
}

// falsly value statements

let falsely = false;
if (falsely) {
    console.log(`${falsely} is truthly value`);
} else {
    console.log(`${falsely} is truthly value`);
}

let blank = "";
if(blank){
    console.log(`${blank} is truthy value`);
}else{
    console.log(`${blank} is falsy value`);
}

let non = 0 ;

if (non) {
    console.log(`${non} is truthly value`);
} else {
    console.log(`${non} is falsy value`);
}